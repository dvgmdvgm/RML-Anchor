# 📂 Session History

This folder contains summaries of past project work sessions.

## File Format

Each session is saved in a file named `YYYY-MM-DD.md`.

## How to Use

When executing `/sleep`, a summary of the current session is automatically saved here.

When executing `/wakeup`, the last session is loaded to restore context.
